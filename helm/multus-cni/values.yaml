name: multus-cni
serviceType: managed

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"

image:
  registry: docker.io
  name: giantswarm/multus-cni
  tag: v3.9-thick-amd64
  pullPolicy: IfNotPresent
